/* =========================================================
   nav.js — wires the sidebar behaviors shared by every page
   (mobile off-canvas toggle, clear-data button). Include this
   after common.js/db.js and before the page's own script.
   ========================================================= */
Common.wireMobileNav();
Common.wireClearDataButton();
